# Model used only by the Python Agent execution service.
MODEL_PROVIDER=remote
MODEL_API_BASE=https://api.deepseek.com
MODEL_API_KEY=
MODEL_NAME=deepseek-v4-flash
LOCAL_MODEL_API_BASE=http://host.docker.internal:8010/v1
LOCAL_MODEL_NAME=local-deepseek-r1-distill-qwen-1.5b
AGENT_MAX_STEPS=16

# Local PostgreSQL and bootstrap login.
POSTGRES_DB=aio_agent
POSTGRES_USER=aio_agent
POSTGRES_PASSWORD=replace_with_a_local_database_password
AIO_BOOTSTRAP_ADMIN_USERNAME=admin
AIO_BOOTSTRAP_ADMIN_PASSWORD=replace_with_an_admin_password

# Use different random values outside local development.
AIO_JWT_SECRET=replace-with-at-least-32-random-bytes
INTERNAL_SERVICE_TOKEN=replace-with-a-random-service-token

# One-time, idempotent import of data/app.sqlite3 into PostgreSQL.
AIO_LEGACY_IMPORT_ENABLED=true
AIO_LEGACY_OWNER_USERNAME=admin
