# HTTPS certificates

Production Nginx expects these two files in this directory:

- `fullchain.pem`
- `privkey.pem`

Use certificates issued for your public domain. Certificate files and private keys are ignored by Git and must never be committed.
