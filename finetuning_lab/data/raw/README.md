# Raw Data

Put original traces, exported conversations, or manually curated examples here.

The first converter reads the project trace file directly:

```text
/Users/bytedance/Documents/aio_agent_framework/traces/latest.jsonl
```

You can also copy stable trace snapshots into this folder before converting them.
